import { FC } from "react";
import { FloatingEmojiType } from "../../atoms/floatingEmojis";

const FloatingEmoji: FC<FloatingEmojiType> = ({
  emoji,
  id,
  xPercent,
  yPercent,
}) => {
  return <div></div>;
};

export default FloatingEmoji;
