import { QueueContentView } from "@components";
import { Content } from "@types";

interface PropsType {
  data: Content;
}

const SoundCloudContent = () => {
  //   return <QueueContentView />;
  return <></>;
};

export default SoundCloudContent;
